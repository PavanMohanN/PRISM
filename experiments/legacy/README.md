# Legacy (pre-revision) pipeline

These are the original single-seed experiment scripts that produced the pre-revision tables T0-T8. They are kept for reference only. The revision pipeline (phase2-phase5 scripts in the parent folder, orchestrated by `run_all_phases.py`) supersedes them.
